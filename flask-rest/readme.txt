# Dockerfile所在目录下
docker build -t imagename:tag .

docker run -d --name <containername> --restart=always -e TIMEZONE=Asis/Shanghai \
-p <外部端口>:5000 <imagename>

apt-get update

apt-get install vi
or
apt-get install vim

